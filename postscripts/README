The files in this directory get combined, in alphabetical order, into a single executable postinst script. The script is prefixed with #!/bin/sh and is placed as control/postinst in the ipk file, and so gets run as part of the installation of the ipk.